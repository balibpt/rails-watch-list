<%= simple_form_for(@list) do |f| %>
  <%= f.input :name, label: "Name of List:", required: false  %>
  <%= f.submit "Create", class: "btn btn-primary"%>
<% end %>
<%= link_to "Back to Lists", lists_path %>
